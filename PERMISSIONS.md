# Permission catalog

<!-- Generated by scripts/generate_permission_matrix.py. Do not edit by hand:
     a test compares this file against the catalog and the route table. -->

Every action this console gates has a name. Routes declare a permission; roles
are bundles of permissions. This file is generated from
[`app/permissions.py`](app/permissions.py) and from the app's own routing table,
so it cannot describe a rule the code does not enforce.

> **These names are contract.** They are what a deployment writes in its
> `ROLES_FILE`. A rename here breaks every role file that used the old name, so
> treat one like any other breaking change: add a new name, never re-point an
> existing one at a different action.

## The three built-in roles

`viewer` reads, `operator` moves money, `admin` additionally holds the release
valves — the actions that undo or erase what another operator did. They are
**fixed**: a `ROLES_FILE` that redefines one is refused at startup, so an
upgrade cannot quietly change what an existing role means.

## The grid

| Permission | viewer | operator | admin | What it allows |
|---|:-:|:-:|:-:|---|
| `console.view` | ✓ | ✓ | ✓ | Read every list and detail page in the console. |
| `export.csv` | ✓ | ✓ | ✓ | Download a list as CSV, including the masked recipient coordinates on it. |
| `onboarding.edit` | · | ✓ | ✓ | Start, fill in, and discard onboarding drafts, and reopen a rejected application for correction. |
| `onboarding.submit` | · | ✓ | ✓ | Submit a completed onboarding draft to Conduit. |
| `onboarding.idv_link` | · | ✓ | ✓ | Fetch a person's identity-verification link (a one-time bearer URL). |
| `onboarding.access_any` | · | · | ✓ | Open onboarding drafts started by another operator (your own always open). |
| `document.upload` | · | ✓ | ✓ | Upload a supporting document and attach it to an onboarding, a payout, a batch or an RFI response. |
| `rfi.respond` | · | ✓ | ✓ | Acknowledge and answer Conduit's requests for information. |
| `account.request` | · | ✓ | ✓ | Request a virtual account for a customer. |
| `payout.create` | · | ✓ | ✓ | Send a payout to a recipient outside the organization. |
| `payout.cancel` | · | ✓ | ✓ | Cancel a payout that has not settled yet. |
| `batch.upload` | · | ✓ | ✓ | Upload a payout batch file, mark it ready, or abandon it before dispatch. |
| `batch.dispatch` | · | ✓ | ✓ | Dispatch a prepared batch: one payout sent to Conduit for every row in it. |
| `transfer.create` | · | ✓ | ✓ | Transfer funds between two Conduit accounts of this organization. |
| `order.create` | · | ✓ | ✓ | Take a conversion quote and create the order behind it. |
| `order.execute` | · | ✓ | ✓ | Execute a pending conversion order at its quoted rate. |
| `order.cancel` | · | ✓ | ✓ | Cancel a pending conversion order and give up its rate. |
| `contact.edit` | · | ✓ | ✓ | Rename a saved contact or change its stored payment coordinates. |
| `contact.archive` | · | ✓ | ✓ | Archive a saved contact so it stops being offered. |
| `contact.delete` | · | · | ✓ | Purge a saved contact's stored coordinates permanently. |
| `whitelist.register` | · | ✓ | ✓ | Register a recipient with Conduit for intercompany payouts. |
| `whitelist.revoke` | · | ✓ | ✓ | Revoke a registered recipient. |
| `operation.retry` | · | ✓ | ✓ | Retry an operation this console left unresolved. |
| `operation.abandon` | · | · | ✓ | Mark an unresolved operation abandoned, releasing its duplicate guard. |
| `sandbox.simulate` | · | ✓ | ✓ | Drive the sandbox's simulation endpoints (approve, settle, reject, deposit). |

## Where each one is enforced

### `console.view`

Read every list and detail page in the console.

*Requires:* nothing — it is the read itself  
*Held by:* viewer, operator, admin  
*Enforced on 40 routes:*

- `GET /`
- `GET /accounts`
- `GET /applications`
- `GET /applications/{application_id}`
- `GET /applications/{application_id}/quick`
- `GET /applications/{application_id}/status`
- `GET /contacts`
- `GET /convert`
- `GET /convert/{operation_id}`
- `GET /customers`
- `GET /customers/{customer_id}`
- `GET /customers/{customer_id}/accounts/{virtual_account_id}`
- `GET /customers/{customer_id}/batches`
- `GET /customers/{customer_id}/batches/new`
- `GET /customers/{customer_id}/batches/template.csv`
- `GET /customers/{customer_id}/batches/{batch_id}`
- `GET /customers/{customer_id}/contacts`
- `GET /customers/{customer_id}/contacts/{cp_id}/history`
- `GET /customers/{customer_id}/convert`
- `POST /customers/{customer_id}/convert/quote`
- `GET /customers/{customer_id}/payouts`
- `GET /customers/{customer_id}/payouts/new`
- `POST /customers/{customer_id}/payouts/quote`
- `GET /customers/{customer_id}/recipients`
- `GET /customers/{customer_id}/request-account`
- `GET /customers/{customer_id}/transfers/new`
- `GET /drafts`
- `GET /onboarding`
- `GET /onboarding/{draft_id}`
- `GET /onboarding/{draft_id}/review`
- `GET /operations/{op_id}`
- `GET /orders`
- `GET /orders/{order_id}`
- `GET /payouts`
- `GET /payouts/contact`
- `GET /payouts/new`
- `GET /rfis`
- `GET /transactions`
- `GET /transactions/{transaction_id}`
- `GET /transfers/new`

### `export.csv`

Download a list as CSV, including the masked recipient coordinates on it.

*Requires:* `console.view`  
*Held by:* viewer, operator, admin  
*Enforced on 1 route:*

- `GET /export/{surface}.csv`

### `onboarding.edit`

Start, fill in, and discard onboarding drafts, and reopen a rejected application for correction.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 7 routes:*

- `POST /applications/{application_id}/resubmit`
- `POST /onboarding`
- `POST /onboarding/{draft_id}/discard`
- `POST /onboarding/{draft_id}/persons`
- `POST /onboarding/{draft_id}/persons/{index}/remove`
- `POST /onboarding/{draft_id}/review`
- `POST /onboarding/{draft_id}/save`

### `onboarding.submit`

Submit a completed onboarding draft to Conduit.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /onboarding/{draft_id}/submit`

### `onboarding.idv_link`

Fetch a person's identity-verification link (a one-time bearer URL).

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /applications/{application_id}/persons/{reference_id}/idv-link`

### `onboarding.access_any`

Open onboarding drafts started by another operator (your own always open).

*Requires:* `console.view`  
*Held by:* admin  
*Enforced on no route:* it gates a record, not a URL — another operator's draft is only readable to someone holding it.


### `document.upload`

Upload a supporting document and attach it to an onboarding, a payout, a batch or an RFI response.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /documents`

### `rfi.respond`

Acknowledge and answer Conduit's requests for information.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 2 routes:*

- `POST /rfis/{rfi_id}/acknowledge`
- `POST /rfis/{rfi_id}/respond`

### `account.request`

Request a virtual account for a customer.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/request-account`

### `payout.create`

Send a payout to a recipient outside the organization.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/payouts/new`

### `payout.cancel`

Cancel a payout that has not settled yet.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /transactions/{transaction_id}/cancel`

### `batch.upload`

Upload a payout batch file, mark it ready, or abandon it before dispatch.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 3 routes:*

- `POST /customers/{customer_id}/batches`
- `POST /customers/{customer_id}/batches/{batch_id}/abandon`
- `POST /customers/{customer_id}/batches/{batch_id}/ready`

### `batch.dispatch`

Dispatch a prepared batch: one payout sent to Conduit for every row in it.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 2 routes:*

- `GET /customers/{customer_id}/batches/{batch_id}/confirm`
- `POST /customers/{customer_id}/batches/{batch_id}/dispatch`

### `transfer.create`

Transfer funds between two Conduit accounts of this organization.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/transfers/new`

### `order.create`

Take a conversion quote and create the order behind it.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 2 routes:*

- `POST /convert/{operation_id}`
- `POST /customers/{customer_id}/convert/select`

### `order.execute`

Execute a pending conversion order at its quoted rate.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /orders/{order_id}/execute`

### `order.cancel`

Cancel a pending conversion order and give up its rate.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /orders/{order_id}/cancel`

### `contact.edit`

Rename a saved contact or change its stored payment coordinates.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 3 routes:*

- `GET /customers/{customer_id}/contacts/{cp_id}/edit`
- `POST /customers/{customer_id}/contacts/{cp_id}/edit`
- `POST /customers/{customer_id}/contacts/{cp_id}/rename`

### `contact.archive`

Archive a saved contact so it stops being offered.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/contacts/{cp_id}/archive`

### `contact.delete`

Purge a saved contact's stored coordinates permanently.

*Requires:* `console.view`  
*Held by:* admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/contacts/{cp_id}/delete`

### `whitelist.register`

Register a recipient with Conduit for intercompany payouts.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/recipients`

### `whitelist.revoke`

Revoke a registered recipient.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /customers/{customer_id}/recipients/{recipient_id}/revoke`

### `operation.retry`

Retry an operation this console left unresolved.

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 1 route:*

- `POST /operations/{op_id}/retry`

### `operation.abandon`

Mark an unresolved operation abandoned, releasing its duplicate guard.

*Requires:* `console.view`  
*Held by:* admin  
*Enforced on 1 route:*

- `POST /operations/{op_id}/abandon`

### `sandbox.simulate`

Drive the sandbox's simulation endpoints (approve, settle, reject, deposit).

*Requires:* `console.view`  
*Held by:* operator, admin  
*Enforced on 5 routes:*

- `POST /applications/{application_id}/simulate`
- `POST /customers/{customer_id}/accounts/{virtual_account_id}/simulate-deposit`
- `POST /customers/{customer_id}/recipients/{recipient_id}/simulate`
- `POST /orders/{order_id}/simulate`
- `POST /transactions/{transaction_id}/simulate`


## What is *not* gated by a permission

Six routes carry no permission, on purpose:

| Route | Why |
|---|---|
| `GET /health/live`, `GET /health/ready` | Deployment probes. They must work without credentials, and they answer with a status and nothing else. |
| `POST /webhooks/conduit` | Conduit's delivery, verified by its signature. There is no operator and no session; an operator permission would be meaningless. |
| `GET /auth/login`, `POST /auth/logout` | The sign-in ceremony itself. Every authenticated actor may end their own session. |
| `GET /customers/{customer_id}/counterparties` | A 301 to the renamed Contacts page. It reads nothing and renders nothing. |

Everything else that changes state — every `POST`, `PUT` and `DELETE` in
`app/web` — declares a cataloged permission, and a test walks the route table on
every run to prove it. An action that shipped without a gate would fail the
suite by name.

Reads are one permission, `console.view`: the console has a single read surface,
and every list and detail page in it has always been gated the same way.
Three GETs are named otherwise: `export.csv`, because a download is not a page —
it takes the same PII off the screen and into a file — and the two pages that
*are* the action rather than a view of it, a contact's edit form (`contact.edit`)
and a batch's dispatch confirmation (`batch.dispatch`).

## Custom roles

Set `ROLES_FILE` to a JSON file:

```json
{
  "payments-clerk": ["console.view", "payout.create", "contact.edit"],
  "batch-dispatcher": ["console.view", "batch.dispatch"],
  "reporting": ["console.view", "export.csv"]
}
```

Map your IdP's groups onto those names with `AUTH_ROLE_MAP` exactly as you would
a built-in role (`AUTH_ROLE_MAP=finance-team=payments-clerk`); an unmapped group
still grants nothing. Roles are deployment config, not a runtime screen: they
live in your own change control, where the diff is reviewed before it ships.

The file is read once, at startup, and **the app refuses to start** rather than
run with a role file it cannot honour:

| Refused | Message names |
|---|---|
| A permission that is not in the catalog | the role, the bad name, and the closest real permission |
| A permission without the reads it requires (*Requires*, above) | the role, the permission, and what is missing |
| An admin-class permission without the whole operator base | the role, the permission, and how many operator permissions are missing |
| A role that grants nothing | the role |
| A role that redefines `viewer`, `operator` or `admin` | the role |
| A file that is unreadable or is not JSON | the setting and the parse error |

Admin-class permissions are the release valves — `contact.delete`, `onboarding.access_any`, `operation.abandon` — plus
`sandbox.simulate` anywhere `CONDUIT_ENV` is not `sandbox`, because off the
sandbox host those routes still post to a real one.

## What the screen says

Where a page withholds an action, it names the action and then the permission
that would allow it — *"Read-only: sending a payout needs the `payout.create`
permission."* — in the same vocabulary as this file, so an operator can quote
the sentence to whoever maintains the role file and that person can find the row
above without translating anything.

No sentence names a role. Which role carries a permission is the deployment's
decision, and the console cannot know it: a custom role may hold
`payout.create` without being `operator`, and telling that operator to "ask an
admin" would be a guess about someone else's org chart. A surface that gates
several actions separately names each one's permission, because a role holds
them one at a time.
